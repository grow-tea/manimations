[project]
name = "manimations"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "manim>=0.19.0",
    "manim-slides>=5.5.2",
]

[tool.uv.workspace]
members = [
    "goniometrie",
]
